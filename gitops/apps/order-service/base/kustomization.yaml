# Base kustomization for order-service
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - service.yaml
  - hpa.yaml

commonLabels:
  app.kubernetes.io/name: order-service
  app.kubernetes.io/part-of: kubeflow-ops
