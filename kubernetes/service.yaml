# =============================================================================
# Service — ClusterIP (internal access, fronted by ALB Ingress)
# =============================================================================
apiVersion: v1
kind: Service
metadata:
  name: cloudpulse-svc
  namespace: cloudpulse
  labels:
    app: cloudpulse
spec:
  type: ClusterIP
  selector:
    app: cloudpulse
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
